// swift-tools-version: 6.1
import PackageDescription

let package = Package(
    name: "MacScreenServer",
    platforms: [
        .macOS(.v12)
    ],
    products: [
        .library(name: "MacScreenServer", targets: ["MacScreenServer"]),
        .executable(name: "SignalingDemo", targets: ["SignalingDemo"])
    ],
    targets: [
        .target(
            name: "MacScreenServer",
            dependencies: []
        ),
        .executableTarget(
            name: "SignalingDemo",
            dependencies: ["MacScreenServer"]
        ),
        .testTarget(
            name: "MacScreenServerTests",
            dependencies: ["MacScreenServer"]
        ),
    ]
)
